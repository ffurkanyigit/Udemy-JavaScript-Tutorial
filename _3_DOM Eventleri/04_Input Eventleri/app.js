const filter=document.getElementById("filter");
/*
document.addEventListener("DOMContentLoaded",load); // "DOMContentLoaded" önemli bir eventtir. Sayfanın yüklenmesi tamamlandıktan sonra veri gelmeye başlar.
function load(e){
    console.log("Sayfa Yüklendi.");
}
*/
// Focus ve Blur Eventi
/*
filter.addEventListener("focus",run); // Input'a girdiğimizde (odaklandığımızda) veri gelir.
filter.addEventListener("blur",run); // Inputtan çıktığımızda (odaktan çıktığımızda) veri gelir.
function run(e){
    console.log(e.type);
}
*/
// Paste Eventi
/*
filter.addEventListener("paste",run); // Input'a ('Bir Todo Arayın' kutusu) bir text yapıştırdığımız zaman bize veri gelmeye başlar. 
function run(e){
    console.log(e.type);
}
*/
// Copy Eventi
/*
filter.addEventListener("copy",run); // Input içinden ('Bir Todo Arayın' kutusu) bir text'i kopyaladığımız zaman bize veri gelmeye başlar.
function run(e){
    console.log(e.type);
}
*/
// Cut Eventi
/*
filter.addEventListener("cut",run); // Input içinden ('Bir Todo Arayın' kutusu) bir text'i kestiğimiz zaman bize veri gelmeye başlar.

function run(e){
    console.log(e.type);
}
*/
// Select Eventi
/*
filter.addEventListener("select",run); // Input içinden ('Bir Todo Arayın' kutusu) bir text'i seçtiğimiz zaman bize veri gelmeye başlar.

function run(e){
    console.log(e.type);
}
*/
